--- Trilinos-trilinos-release-12-14-1/packages/seacas/libraries/aprepro_lib/apr_aprepro.cc	2019-02-27 05:42:22.000000000 -0800
+++ patch-files/apr_aprepro.cc	2019-04-23 18:15:03.943184696 -0700
@@ -700,8 +700,8 @@
                               << "}\t(immutable)" << '\n';
               }
               else if (ptr->type == Parser::token::SVAR) {
-                if (index(ptr->value.svar.c_str(), '\n') != nullptr ||
-                    index(ptr->value.svar.c_str(), '"') != nullptr) {
+                if (index((char *) ptr->value.svar.c_str(), '\n') != nullptr ||
+                    index((char *) ptr->value.svar.c_str(), '"') != nullptr) {
                   (*infoStream) << comment << "  {" << std::left << std::setw(width) << ptr->name
                                 << "\t= '" << ptr->value.svar << "'}" << '\n';
                 }
@@ -711,8 +711,8 @@
                 }
               }
               else if (ptr->type == Parser::token::IMMSVAR) {
-                if (index(ptr->value.svar.c_str(), '\n') != nullptr ||
-                    index(ptr->value.svar.c_str(), '"') != nullptr) {
+                if (index((char *) ptr->value.svar.c_str(), '\n') != nullptr ||
+                    index((char *) ptr->value.svar.c_str(), '"') != nullptr) {
                   (*infoStream) << comment << "  {" << std::left << std::setw(width) << ptr->name
                                 << "\t= '" << ptr->value.svar << "'}\t(immutable)" << '\n';
                 }
